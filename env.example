# ============================================
# Docker Compose 环境变量配置示例
# ============================================
# 使用方法：
# 1. 在项目根目录创建 .env 文件
# 2. 复制以下内容到 .env 文件中
# 3. 根据实际情况修改环境变量的值
# 4. 注意：.env 文件不应提交到版本控制系统
# ============================================

# --------------------------------------------
# MySQL 数据库配置（Docker Compose 使用）
# --------------------------------------------
# MySQL root 用户密码（生产环境请务必修改为强密码）
MYSQL_ROOT_PASSWORD=123456

# MySQL 数据库名称
MYSQL_DATABASE=jimiai

# MySQL 普通用户名称
MYSQL_USER=jimi

# MySQL 普通用户密码（生产环境请务必修改为强密码）
MYSQL_PASSWORD=123456

# MySQL 端口映射（主机端口:容器端口）
# 如果主机端口被占用，可以修改为其他端口，如 3307
MYSQL_PORT=3306

# --------------------------------------------
# Redis 配置（Docker Compose 使用）
# --------------------------------------------
# Redis 端口映射（主机端口:容器端口）
# 如果主机端口被占用，可以修改为其他端口，如 6380
REDIS_PORT=6379

# Redis 密码（生产环境请务必修改为强密码）
REDIS_PASSWORD=123456

# --------------------------------------------
# 应用配置（Docker Compose 使用）
# --------------------------------------------
# 应用服务端口映射（主机端口:容器端口）
# 如果主机端口被占用，可以修改为其他端口
APP_PORT=27355

# ============================================
# 应用配置文件环境变量（config.docker.ini 使用）
# ============================================
# 这些环境变量会被 config.docker.ini 文件读取
# 格式：${VAR:-default} 表示如果环境变量不存在则使用默认值
# ============================================

# --------------------------------------------
# 数据库配置（应用使用）
# --------------------------------------------
# 数据库主机地址（Docker 环境使用服务名，如 mysql）
DB_HOST=mysql

# 数据库端口
DB_PORT=3306

# 数据库用户名
DB_USER=root

# 数据库密码（生产环境请务必修改为强密码）
DB_PASSWORD=123456

# 数据库名称
DB_NAME=jimiai

# 数据库时区
DB_TIMEZONE=Asia/Shanghai

# 数据库表前缀
DB_TABLE_PREFIX=ai_

# --------------------------------------------
# Redis 配置（应用使用）
# --------------------------------------------
# Redis 地址（Docker 环境使用服务名，如 redis:6379）
REDIS_ADDR=redis:6379

# Redis 密码（生产环境请务必修改为强密码）
# 注意：此变量名与上面的 REDIS_PASSWORD 相同，Docker Compose 和配置文件共用

# Redis 数据库编号
REDIS_DB=0

# --------------------------------------------
# 应用服务配置
# --------------------------------------------
# 应用运行端口（格式：:端口号）
# 注意：此变量名与上面的 APP_PORT 不同，这里是应用内部端口
APP_RUN_PORT=:27355

# 管理后台端口
ADMIN_APP_PORT=:27356

# 是否为主服务器
LOCAL_IS_MASTER=true

# --------------------------------------------
# 域名配置
# --------------------------------------------
# API 域名
API_DOMAIN=http://localhost:27355

# H5 页面域名
H5_DOMAIN=http://localhost:27355

# --------------------------------------------
# 七牛云配置（可选）
# --------------------------------------------
# 七牛云 AccessKey（生产环境请务必设置）
QINIU_ACCESS_KEY=

# 七牛云 SecretKey（生产环境请务必设置）
QINIU_SECRET_KEY=

# 七牛云存储桶名称
QINIU_BUCKET=

# 七牛云图片访问域名
QINIU_IMG_HOST=

# --------------------------------------------
# 邮箱配置（可选）
# --------------------------------------------
# SMTP 服务器地址
EMAIL_SMTP_HOST=smtp.qq.com

# SMTP 端口
EMAIL_SMTP_PORT=587

# 发送邮箱地址（生产环境请务必设置）
EMAIL_FROM_EMAIL=

# 发送邮箱密码或授权码（生产环境请务必设置）
EMAIL_FROM_PASSWORD=

# 发送者名称
EMAIL_FROM_NAME=吉米AI

# --------------------------------------------
# 应用 API 配置（可选）
# --------------------------------------------
# Dyu API 基础地址（代码中会自动拼接具体路径）
# 支持的路径：/v1, /v2, /api, /user
APP_DYU_API_URL=https://api.dyuapi.com

# Dyu API 版本号（如 v1, v2）
APP_DYU_API_VERSION=v1
